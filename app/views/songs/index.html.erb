<h1> Songs List </h1>

<% @songs.each do |song| %>
    <h3> <%= link_to song.title, "/songs/#{song.id}" %> </h3>
    <p> Length: <%= song.length %> </p>
    <p> Play Count: <%= song.play_count %> </p>
<% end %>